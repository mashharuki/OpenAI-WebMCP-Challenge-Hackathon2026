# Judging evidence map

The four criteria are treated as equally important. Replace every `TODO` with a
final URL or captured filename before using this map in the submission.

| Criterion | What the judge should understand | Live proof | Repository proof |
| --- | --- | --- | --- |
| WebMCP Leverage | The WebMCP invocation stays pending while a person decides, then the original call receives one structured result | Sponsor golden path and WebMCP result screenshot | `useWebMCPTools.ts`, `gateCoordinator.ts`, fake-host E2E |
| Execution | Sponsor access is wallet-free; payment shows terms and receipt; failures end safely | Public sponsor flow; same-release payment clip if necessary | Release gate, browser E2E, public smoke, deployment runbook |
| Potential Impact | Publishers can preserve a free path while agents access structured premium work | Problem/solution opening and two access choices | Shared protected-resource contract and sponsor/payment evidence model |
| Creativity & Ambition | Sponsor attention and x402 settlement are interchangeable human-approved evidence for one tool | Choice UI followed by identical analysis result | Gate state machine, sponsor grants, payment coordinator, facilitator boundary |

## Evidence inventory

- Live app: `TODO: final HTTPS frontend URL`
- Public repository: <https://github.com/mashharuki/OpenAI-WebMCP-Challenge-Hackathon2026>
- Public video: `TODO: final public YouTube URL`
- Release SHA: `TODO: full final commit SHA`
- Sponsor result screenshot: `TODO: filename`
- Payment receipt screenshot or clip: `TODO: filename and live/recorded label`
- WebMCP host result screenshot: `TODO: filename`

Do not claim a live paid path unless the hosted facilitator has been verified
for that release. A local clip must show the same release SHA and the visible
label `recorded local prototype`.
