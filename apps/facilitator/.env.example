# Secret Base Sepolia facilitator signer key. Set only in the hosting secret store.
EVM_PRIVATE_KEY=

# Dedicated Base Sepolia JSON-RPC endpoint. Keep provider API keys out of Git.
BASE_SEPOLIA_RPC_URL=

# Local default; managed hosts may inject a different PORT.
PORT=4022
