# Public HTTPS URL of the hosted x402 facilitator. Never include credentials.
FACILITATOR_URL=https://facilitator.example.com

# Public recipient address for testnet USDC payments. Replace before deploy.
EVM_ADDRESS=0x0000000000000000000000000000000000000000

# One exact frontend origin. Do not use a wildcard or trailing slash.
ALLOWED_ORIGINS=https://frontend.example.com

# Full deployed git commit SHA. Public provenance; never a secret.
RELEASE_SHA=

# Fixed payment policy (not configurable): eip155:84532, exact scheme,
# Base Sepolia USDC, 10000 base units (0.01 USDC).
